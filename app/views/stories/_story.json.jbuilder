json.extract! story, :id, :in_order_to, :as, :i_want_to, :i_do, :this_happens, :importance, :comments, :dev_comments, :difficulty, :story_kind, :author, :created_at, :updated_at
json.url story_url(story, format: :json)
